// how to convert the object to json string

obj1 = {
  name: "ashutosh",
  age: 22,
  designation: "IAS OFFICER",
};

const json = JSON.stringify(obj1);
console.log(json);
console.log(typeof json);

// express is one the popular package that creates server, these are very helpful in building web applicatiions and APIs....
// localhost refers to own computer , in localhost we access our own environment....

