// package.json ensures a list of packages with their versions
// package-lock.json ensures details of every package installed with their versions and dependencies

// now we are going to understand about server

console.log("server is running");

// function to add two number

function add(a, b) {
  return a + b;
}

var res = add(12, 23);
console.log(res);
